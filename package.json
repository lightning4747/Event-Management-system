{
  "name": "mcet-event-management-system",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "host": "bun --cwd backend dev & bun --cwd frontend host",
    "dev": "bun --cwd backend dev & bun --cwd frontend dev",
    "dev:backend": "bun --cwd backend dev",
    "dev:frontend": "bun --cwd frontend dev",
    "build": "bun --cwd backend build && bun --cwd frontend build",
    "typecheck": "bun --cwd backend build && bun --cwd frontend build",
    "lint": "bun --cwd backend lint",
    "test": "bun --cwd backend test && bun --cwd frontend test",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
